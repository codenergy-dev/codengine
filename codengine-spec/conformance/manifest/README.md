# Manifest conformance

Example manifests that a manifest loader must accept (`valid/`) or reject
(`invalid/`), validated against
[`../../schema/manifest.schema.json`](../../schema/manifest.schema.json).

- `valid/codengine.json` — a project with a default TypeScript module and a Python
  module whose functions live outside the project directory.
