{
  "name": "codengine-cli",
  "version": "0.1.0",
  "description": "Standalone orchestrator: parse a yUML workflow and run it through a language-selected runner.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "codengine": "dist/src/cli.js"
  },
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist/src"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsc && node --test \"dist/test/**/*.test.js\""
  },
  "dependencies": {
    "codengine-loader-ts": "workspace:*",
    "codengine-manifest": "workspace:*",
    "codengine-parser": "workspace:*",
    "codengine-runner-ts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "5.9.2"
  }
}
