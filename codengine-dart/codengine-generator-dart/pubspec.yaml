name: codengine_generator
description: Generate Dart glue that binds task functions for the codengine runner.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  codengine_analyzer:
    path: ../codengine-analyzer-dart
  path: ^1.9.0
